spring:
  datasource:
    username: plants
    password: plants
    url: jdbc:mysql://localhost:3306/plants

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    
  sql:
    init:
      mode: never  